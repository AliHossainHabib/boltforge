import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';

import '../../../../core/di/service_locator.dart';
import '../../../../core/ui/feedback/feedback.dart';
import '../../cubit/{{featureName}}_cubit.dart';
import '../../repository/{{featureName}}_repository.dart';
import '../widgets/{{featureName}}_list_item.dart';

/// Generated by `boltforge generate {{featureName}}`.
///
/// Wire this into your router (`lib/core/router/app_router.dart` and
/// `routes.dart`) once you're ready to navigate to it.
class {{featureNamePascal}}Screen extends StatelessWidget {
  const {{featureNamePascal}}Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return BlocProvider(
      create: (_) => {{featureNamePascal}}Cubit(sl<{{featureNamePascal}}Repository>())..load(),
      child: const _{{featureNamePascal}}View(),
    );
  }
}

class _{{featureNamePascal}}View extends StatelessWidget {
  const _{{featureNamePascal}}View();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('{{featureNameTitle}}')),
      body: BlocBuilder<{{featureNamePascal}}Cubit, {{featureNamePascal}}State>(
        builder: (context, state) {
          return switch (state) {
            {{featureNamePascal}}Initial() || {{featureNamePascal}}Loading() =>
              const AppLoadingView(),
            {{featureNamePascal}}Empty() =>
              const AppEmptyView(title: 'Nothing here yet'),
            {{featureNamePascal}}Error(:final error) => AppErrorView(
                message: error.localizedMessage,
                onRetry: () => context.read<{{featureNamePascal}}Cubit>().load(),
              ),
            {{featureNamePascal}}Loaded(:final items) => ListView.builder(
                itemCount: items.length,
                itemBuilder: (context, index) => {{featureNamePascal}}ListItem(
                  item: items[index],
                ),
              ),
          };
        },
      ),
    );
  }
}
