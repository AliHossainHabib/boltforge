import 'package:flutter/material.dart';

import '../../model/{{featureName}}_model.dart';

/// Generated by `boltforge generate {{featureName}}`.
///
/// Single row for a {{featureNameTitle}} item. Kept as its own widget
/// (rather than an inline `ListTile` in the screen's `builder`) so
/// presentation logic has somewhere to grow — add fields from
/// `{{featureNamePascal}}Model` here as the model gains more of them.
class {{featureNamePascal}}ListItem extends StatelessWidget {
  final {{featureNamePascal}}Model item;
  final VoidCallback? onTap;

  const {{featureNamePascal}}ListItem({
    super.key,
    required this.item,
    this.onTap,
  });

  @override
  Widget build(BuildContext context) {
    return ListTile(
      title: Text(item.id),
      onTap: onTap,
    );
  }
}
