/// Generated by `boltforge generate {{featureName}}`.
///
/// Replace the fields below with your real API shape, then update
/// `{{featureNamePascal}}Repository` and `{{featureNamePascal}}Cubit` to match.
class {{featureNamePascal}}Model {
  final String id;

  const {{featureNamePascal}}Model({required this.id});

  factory {{featureNamePascal}}Model.fromJson(Map<String, dynamic> json) {
    return {{featureNamePascal}}Model(
      id: json['id']?.toString() ?? '',
    );
  }

  Map<String, dynamic> toJson() => {
        'id': id,
      };
}
