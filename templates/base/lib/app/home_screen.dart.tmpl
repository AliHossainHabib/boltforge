import 'package:flutter/material.dart';

/// Placeholder landing screen. Replace with your real home/dashboard —
/// this exists so the generated app has a reachable, working screen after
/// login/verification without you needing to build one on day one.
class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('{{projectNameTitle}}')),
      body: const Center(child: Text('You are signed in.')),
    );
  }
}
