---
name: Bug report
about: Something in boltforge itself didn't work as expected
title: "[Bug] "
labels: bug
---

**Describe the bug**
A clear description of what went wrong.

**Command run**
The exact command, e.g.:
```
boltforge create my_app --auth=password
```

**Full output**
Paste the complete terminal output, including the error text.

```
(paste here)
```

**Expected behavior**
What you expected to happen instead.

**Environment**
- OS: [e.g. Windows 11, Ubuntu 22.04, macOS 14]
- `dart --version`:
- `flutter --version`:
- `boltforge doctor` output:

**Additional context**
Anything else relevant — e.g. was this a fresh project, or did you run
`generate` on an existing one? Any manual edits to `service_locator.dart`
or the generated templates beforehand?
